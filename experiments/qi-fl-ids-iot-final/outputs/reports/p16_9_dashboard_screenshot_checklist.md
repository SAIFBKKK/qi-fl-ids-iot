# P16.9 Dashboard Screenshot Checklist

Capture these screenshots for the final defense evidence package.

- Dashboard overview in light mode.
- Dashboard overview in dark mode.
- Device connected notification toast.
- Node Arrival Stream with recent device events.
- Device table with `iot-rpi-weak` and `iot-smart-watch-medium`.
- Model Profile panel showing `p8_fedavg_qga_l1` and `conservative_seed_42`.
- Alert detected notification toast.
- Recent Alerts / Security Events panel.
- Service Status panel with controller, validator, bridge, and final IDS API.
- MQTT topic counts / bridge evidence visible.
- `/nodes` and `/assignments` browser tabs if useful.
- Grafana plus dashboard side-by-side if useful for the presentation.
