# P16.12 Real-Traffic Passive Observation Report

## Objective

P16.12 adds a passive observation path for VM1:

`passive network observation -> PacketWindow(30) -> 28 features -> scaler JSON -> MQTT -> IDS -> dashboard alert`

## Modules Added

- `experiments/live_lab/realtime_agent/passive_capture.py`
  - `list_interfaces()`
  - `validate_interface(interface_name)`
  - `build_safe_capture_filter(node_ip, peer_ip=None)`
  - `passive_packet_source(interface_name, max_packets, timeout_seconds, node_ip, peer_ip=None)`

## Modules Modified

- `experiments/live_lab/realtime_agent/run_realtime_window_agent.py`
  - added `--source {synthetic,pcap,live}`
  - added `--node-ip`
  - added `--peer-ip`
  - added `--timeout-seconds`
  - enforced `--allow-live-capture` for live source
  - added passive observation metadata to live MQTT payloads

## Wrapper

- `experiments/qi-fl-ids-iot-final/src/scripts/16_12_run_passive_observation_publish.py`
  - uses `source=live`
  - defaults to VM1 host-only values
  - refuses live mode without `--allow-live-capture`

## CLI Arguments

- `--broker`
- `--node-id`
- `--interface`
- `--node-ip`
- `--peer-ip`
- `--input-mode`
- `--window-size`
- `--max-windows`
- `--timeout-seconds`
- `--dry-run`
- `--publish`
- `--allow-live-capture`

## Security

- Passive observation only.
- No traffic is generated by the agent.
- No scenario is launched.
- Capture filter is limited to the `192.168.56.0/24` host-only lab network and the selected node IP.
- Application payloads are not stored.
- No pcap is written by default.

## Validation Expected

- `passive_capture.py` compiles.
- `run_realtime_window_agent.py` compiles.
- P16.12 wrapper compiles.
- P16.12 setup validation returns `ok=true`.
- Integration tests pass.

## Limits

- Feature extraction remains approximate.
- CICIoT2023 equivalence is not claimed.
- Live capture requires privileges and `scapy` inside VM1.
- Runtime scientific performance remains P12/P13.

## Next Step

P16.13 should collect passive observation evidence from VM1 with benign host-only traffic and dashboard screenshots.
