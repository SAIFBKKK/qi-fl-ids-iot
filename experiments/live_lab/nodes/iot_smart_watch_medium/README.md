# iot-smart-watch-medium

Smart-watch-like medium VM placeholder for P16.1.

Planned role:

- publish 28 scaled features during replay validation;
- prepare future edge inference for the medium node;
- still support server-side MQTT prediction and alert observation.

Step 0 command:

```bash
python run_node.py --dry-run
```

No edge model is loaded and no network activity is started in this step.

