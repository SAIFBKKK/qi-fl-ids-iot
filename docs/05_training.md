# Training

The final training path is under:

```text
experiments/qi-fl-ids-iot-final/
```

The final selected practical configuration is:

```text
FedAvg + QGA
12 selected features
```

Training categories:

- centralized baselines
- FedAvg federated baseline
- FedAvg + QGA final selected path
- QIFA research variants
- robustness and compression analysis

Generated models, logs, reports, and figures are not kept in GitHub. Restore external artifacts only when exact experiment evidence is needed.

Do not run full training without the dataset package and a reviewed local environment.
